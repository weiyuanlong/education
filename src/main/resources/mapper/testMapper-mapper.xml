<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.education.official.mapper.TestMapper">

    <select id="findData" resultType="map">
        SELECT * FROM  sim_info WHERE id = 1
    </select>

</mapper>